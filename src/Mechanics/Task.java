package Mechanics;

/**
 * Simple interface, which enables passing blocks of code as
 * lambda function argument or executing it at any given time.
 * @author Rafal Lazicki
 */
public interface Task {
    void execute();
}
